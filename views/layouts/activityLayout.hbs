<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title></title>
    <link media="all" href="/css/bootstrap.css" rel="stylesheet">
    <link media="all" href="/css/non-responsive.css" rel="stylesheet">
    <link media="all" href="/css/style.css" rel="stylesheet">
    <link media="all" href="/css/uploadifive.css" rel="stylesheet">
    <link href="//cdn.bootcss.com/animate.css/3.4.0/animate.min.css" rel="stylesheet">
    {{#block "css"}}{{/block}}
    <!--[if lt IE 8]>
    <script src="/js/respond.js" type="text/javascript"></script>
    <script src="//cdn.bootcss.com/es5-shim/4.3.1/es5-shim.js"></script>
    <![endif]-->
</head>
<body>
{{> layouts/header}}
<div class="wrapper">
    <div class="container">
        <div class="row">

            <div class="col-xs-24">
                <div class="bg-white activity-info shadow">
                    <div class="create-title bg-success">填写活动信息</div>
                    <form class="form-horizontal m-t-lg m-b-lg" id="activityForm" method="post"
                          action="/activities/publish">
                        <input type="hidden" name="activity.id" value="{{activity.id}}" id="activityId">

                        <div class="form-group m-t-lg">
                            <label class="control-label col-xs-3">活动名称：</label>

                            <div class="col-xs-15">
                                <input class="form-control input-lg" type="text" name="activity.name"
                                       placeholder="例如：北京海淀区中关村">
                            </div>
                        </div>

                        {{#block "extras"}}
                        {{!--活动附加域信息--}}
                        {{/block}}
                        {{> cover }}  {{!-- 封面图片 --}}
                        {{> tags/tagList}} {{!-- 标签 --}}
                        {{{ body }}}   {{!-- 活动内容区域 --}}
                    </form>
                </div>
            </div>
            {{> enroll }}
            {{> vote }}
            <div class="col-xs-24">
                <div class="text-center">
                    <button type="button" class="btn btn-pink m-r-sm publish-btn"><span
                            class="glyphicon glyphicon-ok m-r-xs"></span>发布
                    </button>
                    <button type="button" class="btn btn-default"><span
                            class="glyphicon glyphicon-list-alt m-r-xs"></span>另存为草稿
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
{{> layouts/footer}}
{{#block "scripts"}}{{/block}}
</body>
</html>